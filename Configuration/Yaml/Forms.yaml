imports:
  - { resource: './FormElements/Form.yaml' }
  - { resource: './Finishers/Webhook.yaml' }

TYPO3:
  CMS:
    Form:
      persistenceManager:
        allowedExtensionPaths:
          10: 'EXT:portal/Resources/Private/Forms/'
